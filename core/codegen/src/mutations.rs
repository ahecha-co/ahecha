// We will have here macros to help us to get the strong typed mutation query path
// mutation!(api::query::__id__::post)
// This macro should return a function where you can set the params of the path if any and send the
// request to the server, the mutation queries takes also the form data as parameter.
